@if(Request.IsAuthenticated) {
    <text>Bem-vindo(a) <strong>@User.Identity.Name</strong>!
     @Html.ActionLink("Log Off", "LogOff", "Account", null, new {  }) </text>
}
else
{
    @Html.ActionLink("Log On", "LogOn", "Account", null, new {  }) 
}
